type Query {
    hello: String
  }

type Subscription {
  messageSent: String
}

type Subscription {
  nuevoEvento(cedula: String): Evento
}

type Evento {
  id: ID!
  tipo_mensaje: String!
  mensaje: String!
}
