type Query {
    hello: String
}

type Query {
    Conectados: String
}

type Query {
  GetBuffer: String
}

type Query {
    sendMessage(
      abonado: String!, 
      tipo_mensaje: String!, 
      app: String!, 
      mensaje: String!
    ): String
}

  

type Subscription {
  messageSent: String
}

type Subscription {
  nuevoEvento(cedula: String): Evento
}

type Evento {
  app: String
  tipo_mensaje: String
  mensaje: String
}
